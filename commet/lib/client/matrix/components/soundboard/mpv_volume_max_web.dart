import 'package:media_kit/media_kit.dart';

/// The web player is an <audio> element; its volume is capped at 1.0 and
/// there is no `volume-max` to raise.
Future<void> raiseMpvVolumeMax(Player player, double max) async {}
