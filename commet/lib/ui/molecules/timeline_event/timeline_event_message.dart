import 'package:flutter/material.dart';

class TimelineEventMessage extends StatelessWidget {
  const TimelineEventMessage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
